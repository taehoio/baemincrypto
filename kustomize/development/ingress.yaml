kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: baemincrypto-grpc
  namespace: development
  labels:
    app: baemincrypto-grpc
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/backend-protocol: GRPC
spec:
  rules:
    - host: grpc-development.taeho.io
      http:
        paths:
          - path: "/v1.baemincrypto.Baemincrypto/"
            pathType: Prefix
            backend:
              service:
                name: baemincrypto
                port:
                  number: 8080

---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: baemincrypto-http
  namespace: development
  labels:
    app: baemincrypto-http
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: baemincrypto-development.taeho.io
      http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: baemincrypto
                port:
                  number: 18080
